class Pokemon {
    number;
    name;
    type = "";  // Tipo principal (o primeiro da lista)
    types = []; // Lista com todos os tipos do Pokémon
    photo;
    stats = [];  // Lista com os nomes das estatísticas
    statsNum = []; // Lista com os valores das estatísticas
}
